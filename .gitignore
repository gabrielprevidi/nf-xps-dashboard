# Dependências e build
app/node_modules
app/dist
app/dist-ssr

# Segredos — nunca commitar
app/.env.local
app/.env*.local

# Cache local do Supabase CLI (metadados de link, não segredos, mas não é útil versionar)
supabase/.temp/

# Editor / SO
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
